import 'package:flutter/material.dart';
class SubItemBox extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
        'this is sub item',
        style: TextStyle(
          color: Colors.white,
        ),
      ),
    );
  }
}
